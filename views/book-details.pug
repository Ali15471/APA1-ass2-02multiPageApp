//- This line extends the main layout (layout.pug)
//- It means this template will inherit the structure defined in layout.pug
//- The content defined here will be inserted into the 'block content' of the layout
extends layout

block content
  //- Demonstrating a detailed view with headings and paragraphs
  h2= book.title
  .book-details
    //- Using paragraphs (p) with strong tags for emphasis
    p 
      strong Author: 
      | #{book.author}
    p 
      strong Year: 
      | #{book.year}
    p 
      strong Genre: 
      | #{book.genre}
  
      form(action=`/remove-book/${book.id}`, method="POST")  
      button.remove(type="submit") Remove book  
  //- Anchor tag (a) for navigation back to the book list
  a(href="/") Back to Book List

